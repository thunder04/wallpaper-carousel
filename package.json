{
    "main": "dist/index",
    "type": "module",
    "private": true,
    "dependencies": {
        "esbuild": "^0.13.2",
        "petitio": "^1.3.2"
    },
    "scripts": {
        "build": "npx esbuild index.mjs --minify --bundle --external:petitio --outdir=dist --platform=node --target=esnext --format=esm",
        "watch": "npm run build -- --watch"
    }
}